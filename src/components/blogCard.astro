---
export interface Props {
	title: string;
	description: string;
	date: string;
	url: string;
}

const { title, description, date, url } = Astro.props;
---

<!-- This looked okay--  hover:bg-violet-400 -->
<a
	href={url}
	class="container my-2 flex flex-row items-center justify-between from-rose-400 to-sky-300 p-2 hover:bg-gradient-to-r"
>
	<div>
		<h3 class="text-lg md:text-2xl">{title}</h3>
		<h5 class="text-sm">{date}</h5>
		<p class="hidden text-xl md:block">{description}</p>
	</div>
	<img src="arrow.svg" alt="Read More" class="" />
</a>
